{
  "buildCommand": "cd inventory-system-app && npm run build",
  "outputDirectory": "inventory-system-app/dist",
  "installCommand": "cd inventory-system-app && npm install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}